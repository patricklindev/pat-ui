import React, { FC } from "react"
import { classNames } from '../../utils/classNames';

export interface IPaginationProps {
    className?: string;
    count?: number;
    disabled?: boolean;
}

export const Pagination: FC<IPaginationProps> = (props: any) => {
    return (<div></div>)
}

export default Pagination;
