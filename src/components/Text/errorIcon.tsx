import React from 'react';
import errorIcon from '../../asset/icon/error.svg';

const ErrorIcon = () => {
  return <img className='text__error-icon' src={errorIcon} alt="error icon" />;
};

export default ErrorIcon;
